@model IEnumerable<BTL.Models.TaiKhoan>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.loaiTaiKhoan1.level)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tenDangNhap)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.password)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.loaiTaiKhoan)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.loaiTaiKhoan1.level)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tenDangNhap)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.password)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.loaiTaiKhoan)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.maTaiKhoan }) |
            @Html.ActionLink("Details", "Details", new { id=item.maTaiKhoan }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.maTaiKhoan })
        </td>
    </tr>
}

</table>
