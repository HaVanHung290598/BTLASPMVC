<div class="header">
    <div class="bg-light py-4">
        <div class="row d-flex align-items-center justify-content-between container">
            <div class="col-6 p-0">
                <ul class="d-flex align-items-center p-0">
                    <li class="me-3"><a href="#"><i class="fa fa-phone me-1"></i>0339526324</a></li>
                    <li><a href="#"><i class="fa fa-envelope me-1"></i> DoDaTamAnh@gmail.com</a></li>
                    <img style="justify-content: center" src="https://tamanh.net/wp-content/uploads/2018/02/logo-tamanh-02-1.png" alt="Thế Giới Đồ Da Tâm Anh" />
                    </ul>
                </div>
                <div class="col-6 p-0">
                    <ul class="menu_li p-0 justify-content-lg-end d-flex">
                        <li><a href="#"><i class="fab fa-facebook-f"></i></a></li>
                        <li><a href="#"><i class="fab fa-twitter"></i></a></li>
                        <li><a href="#"><i class="fab fa-linkedin-in"></i></a></li>
                        <li><a href="#"><i class="fab fa-dribbble"></i></a></li>
                        <li><a href="#"><i class="fab fa-google-plus-g"></i></a></li>
                    </ul>
                </div>
            </div>
        </div>
        <div class="d-flex align-items-center container justify-content-between">
            <div class="logo">
                <img src="~/wwwroot/images/home/img_logo.PNG" alt="">
            </div>
            <div class="menu2">
                <ul style="padding: 20px; font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; font-weight: 600">
                    @if (Session["Email"] != null)
                    {
                        <li><a href="@Url.Action("TaiKhoan", "Home")"><i class="fas fa-user"></i> Tài khoản</a></li>

                    }

                    @if (Session["Email"] != null)
                    {
                        <li><a href="@Url.Action("GioHang", "Home")"><i class="fas fa-sign-in-alt"></i> Giỏ Hàng</a></li>
                        <li><a href="@Url.Action("DonHang", "Home")"><i class="fas fa-sign-in-alt"></i> Đơn Hàng</a></li>
                        <li>Xin Chào, @Session["Email"]</li>
                        <li>@Html.ActionLink("Đăng Xuất", "DangXuat", "Home", htmlAttributes: new { @style = "front-size:small" })</li>
                    }
                    else
                    {
                        <li><a href="@Url.Action("GioHang", "Home")"><i class="fas fa-sign-in-alt"></i> Giỏ Hàng</a></li>
                        <li><a href="@Url.Action("DangNhap", "Home")"><i class="fas fa-sign-in-alt"></i> Đăng nhập</a></li>
                        <li><a href="@Url.Action("register", "Home")"><i class="fas fa-sign-in-alt"></i> Đăng Ký</a></li>
                    }
                    @{
                        if (HttpContext.Current.Session["loaiTaiKhoan"] != null && HttpContext.Current.Session["loaiTaiKhoan"].ToString() == "admin")
                        {
                            <a href="@Url.Action("Profile", "Profile", new { area = "Admin" })">Admin</a>
                        }
                    }
                </ul>
            </div>
        </div>

    </div>
